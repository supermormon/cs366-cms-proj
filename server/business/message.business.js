function getMessages (req, res) {
  res.status(200).json({response: "implement message api!"});
}

function getMessage (req, res) {
  res.status(200).json({response: "implement message api!"});
}

function createMessage (req, res) {
  res.status(200).json({response: "implement message api!"});
}

function updateMessage (req, res) {
  res.status(200).json({response: "implement message api!"});
}

function deleteMessage (req, res) {
  res.status(200).json({response: "implement message api!"});
}

module.exports = {
  getMessages,
  getMessage,
  createMessage,
  updateMessage,
  deleteMessage
}