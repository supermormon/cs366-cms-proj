function getContacts(req, res) {
  res.status(200).json({response: "implement contact api!"});
}

function getContact(req, res) {
  res.status(200).json({response: "implement contact api!"});
}

function createContact(req, res) {
  res.status(200).json({response: "implement contact api!"});
}

function updateContact(req, res) {
  res.status(200).json({response: "implement contact api!"});
}

function deleteContact(req, res) {
  res.status(200).json({response: "implement contact api!"});
}

module.exports = {
  getContacts,
  getContact,
  createContact,
  updateContact,
  deleteContact
}